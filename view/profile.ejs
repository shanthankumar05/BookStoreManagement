<html>
    <head>
        <title>Hello User!</title>
        <style>
            body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-image: url("../static/profimg2.jpg");
            background-size: cover;
             }
            h1 {
            background-color: whitesmoke;
            color: black;
            padding: 20px;
            margin: 0;
            }
            h1::after {
                content: '👋'; /* You can replace this with an image or other animation */
                display: inline-block;
                font-size: 2rem;
                animation: wave 2s infinite; /* Waving animation */
            }
            @keyframes wave {
                0%, 100% {
                    transform: rotate(0deg);
                }
                25% {
                    transform: rotate(30deg);
                }
                50% {
                    transform: rotate(0deg);
                }
                75% {
                    transform: rotate(-30deg);
                }
            }
            
            p {
            font-size: 25px;
            margin: 20px;
            padding: 10px;
            color: black;
            text-align: center;
            }
            button {
                background-color: whitesmoke;
                color: black;
                border: none;
                padding: 10px 20px;
                border-radius: 50px;
                cursor: pointer;
                margin-left:15px;
            }
            button:hover{
                opacity:0.8;
                color: red;
            }
            form{
                display:inline-block;
            }
            .buttons{
                display:flex;
                flex-direction: row;
                justify-content: center;
            }
           
            </style>
    </head>
    <body>
        <%- include('layout') %>
        <h1>Hello User!</h1>
        <h2 style="text-align: center; box-shadow:1px 1px 10px brown; color:blue ; font-family:cursive">Welcome To RGM   Online Book Store</h2>
        <div class="box">
        <p><strong>Name: </strong><%=user_details.name%></p>
        <p><strong>Email: </strong><%=user_details.email%></p>
        <div class="buttons">
        <a href="/profile/get-books"><button class="get-books">Get books</button></a>
        <a href="/my-cart"><button class="my-cart">My Cart</button></a>
        <a href="/get-update-profile"><button>Update Profile</button></a>
        <form action="/logout" method="POST">
            <button type="submit" class="logout">Log Out</button>
        </form>
    </div>
    </div>
    </body>
</html> 